import {Action} from '@ngrx/store';
import {Category, Genre, Movie} from '../../../../shared/types';
import {Comment, CommentsByMovie} from '../../../type';

// Add constant GET_MOVIES - GET_CATEGORIES - GET_GENRES

/**
 * Every action is comprised of at least a type and an optional
 * payload. Expressing actions as classes enables powerful
 * type checking in reducer functions.
 *
 * See Discriminated Unions: https://www.typescriptlang.org/docs/handbook/advanced-types.html#discriminated-unions
 */

// Add actions : GetMoviesAction - GetCategoriesAction - GetGenresAction


/**
 * Export a type alias of all actions in this action group
 * so that reducers can easily compose action types
 */
export type Actions = ''; // add all actions here
